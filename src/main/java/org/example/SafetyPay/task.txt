/**
 * ЗАДАЧА: Реализовать "безопасную" оплату с защитой от дубликатов.
 *
 * Контекст:
 * Представьте, что у вас есть API оплаты.
 * Клиент может отправить один и тот же запрос несколько раз (или может быть вызван retry).
 *
 * Проблема:
 * Если не защититься — деньги спишутся несколько раз.
 *
 * Ваша задача:
 * Сделать метод pay(...) идемпотентным.
 *
 * ---------------------------
 *
 * Шаг 1:
 * Реализуйте метод pay(double amount), который уменьшает баланс.
 *
 * Шаг 2:
 * Проблема: что если запрос выполнился случайно 2 раза из-за сетевых сбоев?
 * Как не списать баланс 2 раза?
 *
 * Реализуйте идемпотентность
 *
 * ---------------------------
 *
 * Ожидаемое поведение:
 *
 * pay(100);
 * pay(100);
 *
 * => баланс уменьшился только один раз
 *
 * ---------------------------
 *
 * Шаг 3 :
 * Запустите метод из двух потоков. Логика должна быть потокобезопасной.
 *
 * Усложнение: синхронизироваться только на ключи, а не всю мапу целиком (объясню, если успеем дойти до этого пункта)
 *
 * Финальный результат:
 * - нет дублей транзакции
 * - корректная работа в многопоточности
 *
 */
public class PaymentService {

  private static double balance = 10_000;

  public static void main(String[] args) throws Exception {
    // TODO: протестировать решение
  }

  static void pay(double amount) {
    // TODO: реализовать
  }
 */